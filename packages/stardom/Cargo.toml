[package]
name = "stardom"
version = "0.1.0"
edition = "2021"
authors.workspace = true
repository.workspace = true
license.workspace = true

[dependencies]
stardom-reactive = { path = "../stardom-reactive" }
stardom-nodes = { path = "../stardom-nodes" }
stardom-macros = { path = "../stardom-macros" }

stardom-web = { path = "../stardom-web" }
stardom-render = { path = "../stardom-render" }
